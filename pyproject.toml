[project]
name = "your-project"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  # --- Core DL ---
  "torch>=2.0",

  # --- Scientific stack ---
  "numpy>=1.24",
  "h5py>=3.11",
]
